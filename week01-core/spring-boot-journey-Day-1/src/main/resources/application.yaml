# Server configuration
server:
  port: 8080

# Logging configuration
logging:
  level:
    org:
      springframework: INFO
    com:
      yourname: DEBUG

# JPA configuration (not using DB yet, but good to have)
spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false